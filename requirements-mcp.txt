# Optional external MCP consumer dependencies.
# Install after base requirements.txt so starlette 0.48.0 stays pinned for Gradio 6.10.
#
#   pip install -r requirements.txt
#   pip install -r requirements-mcp.txt
#
# Pin note: mcp 1.27+ pulls sse-starlette 3.x which wants starlette>=1.x.
# Use sse-starlette 2.2.x and re-pin starlette==0.48.0 (already in requirements.txt).

langchain-mcp-adapters==0.2.2
mcp==1.27.2
sse-starlette==2.2.1
