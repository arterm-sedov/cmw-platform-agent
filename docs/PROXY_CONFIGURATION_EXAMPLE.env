# CMW Platform Agent - Proxy Configuration Example
# ================================================
# This file demonstrates how to configure proxy settings for the CMW Platform Agent.
# It supports separate configurations for the agent's internal requests/tools
# and for the Large Language Model (LLM) providers.

# Agent Proxy Configuration (for requests and tools)
# --------------------------------------------------
# Enable or disable the agent's proxy. Set to 'true' to enable.
AGENT_PROXY_ENABLED=false

# HTTP proxy for agent's outgoing HTTP requests
# Example: http://user:pass@your-agent-proxy.com:8080
# AGENT_PROXY_HTTP_PROXY=http://agent-proxy.company.com:8080

# HTTPS proxy for agent's outgoing HTTPS requests
# Example: https://user:pass@your-agent-proxy.com:8080
# AGENT_PROXY_HTTPS_PROXY=http://agent-proxy.company.com:8080

# Comma-separated list of hosts that should bypass the agent's proxy.
# Use domain names or IP addresses. No protocols (http://, https://) or trailing slashes.
# Example: localhost,127.0.0.1,.internal.domain.com,your-local-server
AGENT_PROXY_NO_PROXY=localhost,127.0.0.1,ubuntu-vm-server-gen2

# Whether to verify SSL certificates for agent's proxy connections.
# Set to 'false' to disable (use with caution, e.g., for self-signed certs).
# Default: true (when not specified)
# AGENT_PROXY_VERIFY_SSL=true

# Timeout in seconds for agent's proxy connections.
# Default: 30.0 (when not specified)
# AGENT_PROXY_TIMEOUT=30.0

# LLM Proxy Configuration (for LLM providers)
# --------------------------------------------
# Enable or disable the LLM's proxy. Set to 'true' to enable.
LLM_PROXY_ENABLED=true

# HTTP proxy for LLM's outgoing HTTP requests
# Example: http://user:pass@your-llm-proxy.com:8080
LLM_PROXY_HTTP_PROXY=http://127.0.0.1:18080

# HTTPS proxy for LLM's outgoing HTTPS requests
# Example: https://user:pass@your-llm-proxy.com:8080
LLM_PROXY_HTTPS_PROXY=http://127.0.0.1:18080

# Comma-separated list of hosts that should bypass the LLM's proxy.
# Use domain names or IP addresses. No protocols (http://, https://) or trailing slashes.
# Example: localhost,127.0.0.1,.internal.domain.com,your-llm-server
LLM_PROXY_NO_PROXY=localhost,127.0.0.1

# Whether to verify SSL certificates for LLM's proxy connections.
# Set to 'false' to disable (use with caution, e.g., for self-signed certs).
# Default: true (when not specified)
# LLM_PROXY_VERIFY_SSL=true

# Timeout in seconds for LLM's proxy connections.
# Default: 30.0 (when not specified)
# LLM_PROXY_TIMEOUT=30.0

# Global/Fallback Proxy Configuration (Standard environment variables)
# These are used if specific AGENT_PROXY_ or LLM_PROXY_ are not set.
# ------------------------------------------------------------------------------
# HTTP_PROXY=http://global-proxy:8080
# HTTPS_PROXY=https://global-proxy:8080
NO_PROXY=localhost,127.0.0.1,ubuntu-vm-server-gen2
# REQUESTS_CA_BUNDLE=/path/to/your/cert.pem
# CURL_CA_BUNDLE=/path/to/your/cert.pem

# Authentication Examples
# -----------------------
# AGENT_PROXY_HTTP_PROXY=http://username:password@proxy.company.com:8080
# LLM_PROXY_HTTPS_PROXY=https://username:password@proxy.company.com:8080

# SOCKS Proxy Examples
# --------------------
# AGENT_PROXY_HTTP_PROXY=socks5://proxy.company.com:1080
# LLM_PROXY_HTTPS_PROXY=socks5://proxy.company.com:1080

# SSL Configuration
# -----------------
# AGENT_PROXY_VERIFY_SSL=false  # For self-signed certificates
# LLM_PROXY_VERIFY_SSL=false    # For self-signed certificates

# Debug Configuration (Development Only)
# --------------------------------------
# These are for development/testing only - remove in production
# CMW_DEBUG_MODE=true
# CMW_VERBOSE_LOGGING=true
