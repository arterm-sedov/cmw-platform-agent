# Scratch (platform repo)

**Do not** store instance-specific Volga / mz-tr / mz-fr migration artifacts here.

Use [**my-building**](file:///D:/Repo/my-building) `docs/_scratch/` for harvest JSON, one-off phase runners, and TR→FR id maps. Link from `localization/migration_progress/*.json` via `meta.harvest_path` / `meta.seed_path`.

Reusable automation belongs in `.agents/skills/cmw-platform/scripts/` (see [scripts_index](../.agents/skills/cmw-platform/references/scripts_index.md)).
