# Scratch (platform repo)

**Do not store Volga / mz-tr / mz-fr migration artifacts here.**

Instance-specific harvest JSON, phase scripts, and batch run outputs belong in [**my-building**](file:///D:/Repo/my-building) under `docs/_scratch/`, with progress in `localization/migration_progress/`.

Reusable automation lives in `.agents/skills/cmw-platform/scripts/` (see [scripts_index](../../.agents/skills/cmw-platform/references/scripts_index.md)).
